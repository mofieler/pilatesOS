// Initialize environment validation at startup
import './env-validation';

// This file ensures environment validation happens when the module is loaded
// It should be imported early in the application lifecycle (e.g., in layout.tsx or middleware)
